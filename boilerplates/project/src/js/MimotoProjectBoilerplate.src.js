/**
 * {{PROJECT_NAME}}
 * @author - {{PROJECT_AUTHOR}} ({{PROJECT_EMAIL}})
 */

window.{{PROJECT_ID}} = function()
{
    // 1. helper
    if (console && console.log) console.log('{{PROJECT_NAME}} - Javascript is running! Run "npx webpack" to compile your own JS file"');
}
